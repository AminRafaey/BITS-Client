import InboxIcon from './icon.svg';
import PropTypes from 'prop-types';

InboxIcon.defaultProps = {
  color: '#FFFFFF',
};

InboxIcon.propTypes = {
  color: PropTypes.string,
};

export default InboxIcon;
